spring:
  datasource:
    url: jdbc:h2:mem:payflowdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

server:
  port: 8080

logging:
  level:
    org.springframework: INFO

jwt:
  secret: "change-me-please-change-me-please-123456"
  expirySeconds: 86400
